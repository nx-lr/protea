function-names:
  comment: |
    Node.JS specific Utilities
      Assert
      Atomic
      Buffer
      Cluster
      Console
      CorePack
      Crypto
      ChildProcess
      DataView
      Debugger
      Diagnostics
      Dns
      Domain
      Error
      Event
      Global
      Http
      Inspector
      Intl
      Net
      Os
      Path
      Performance
      Policy
      Process
      Promise
      Proxy
      Punycode
      Query
      Readline
      Reflect
      Repl
      Report
      Stream
      StringDecoder
      Timer
      Tty
      Trace
      Tls
      Udp
      Url
      Util
      V8Engine
      Vm
      WebAssembly
      Zlib
  patterns:
    - comment: Array/List methods
      name: support.function.array.sa
      match: !!set # JavaScript
        ? at
        ? concat
        ? copyWithin
        ? entries
        ? every
        ? fill
        ? filter
        ? find
        ? findIndex
        ? flat
        ? flatMap
        ? forEach
        ? from
        ? includes
        ? indexOf
        ? is
        ? join
        ? keys
        ? lastIndexOf
        ? map
        ? of
        ? pop
        ? push
        ? reduce
        ? reduceRight
        ? reverse
        ? shift
        ? slice
        ? some
        ? sort
        ? splice
        ? toLocaleString
        ? toSource
        ? toString
        ? unshift
        ? values

        # Lodash
        ? chunk
        ? compact
        ? difference
        ? differenceBy
        ? differenceWith
        ? drop
        ? dropRight
        ? dropRightWhile
        ? dropWhile
        ? findLastIndex
        ? first
        ? flatten
        ? flattenDeep
        ? flattenDepth
        ? fromPairs
        ? head
        ? initial
        ? intersection
        ? intersectionBy
        ? intersectionWith
        ? last
        ? nth
        ? pull
        ? pullAll
        ? pullAllBy
        ? pullAllWith
        ? pullAt
        ? remove
        ? sortedIndex
        ? sortedIndexBy
        ? sortedIndexOf
        ? sortedLastIndex
        ? sortedLastIndexBy
        ? sortedLastIndexOf
        ? sortedUniq
        ? sortedUniqBy
        ? tail
        ? take
        ? takeRight
        ? takeRightWhile
        ? takeWhile
        ? union
        ? unionBy
        ? unionWith
        ? uniq
        ? uniqBy
        ? uniqWith
        ? unzip
        ? unzipWith
        ? without
        ? xor
        ? xorBy
        ? xorWith
        ? zip
        ? zipObject
        ? zipObjectDeep
        ? zipWith

        # Flix
        ? append
        ? count
        ? dropLeft
        ? dropWhileLeft
        ? dropWhileRight
        ? exists
        ? filterMap
        ? findIndexOf
        ? findIndexOfLeft
        ? findIndexOfRight
        ? findIndices
        ? findLeft
        ? findMap
        ? findRight
        ? flatMap2
        ? fold
        ? fold2
        ? foldLeft
        ? foldLeft2
        ? foldRight
        ? foldRight2
        ? forall
        ? foreach
        ? foreachAccum
        ? foreachWithIndex
        ? formatWith
        ? get
        ? groupBy
        ? indexOfLeft
        ? indexOfRight
        ? indices
        ? init
        ? init2
        ? intercalate
        ? intersperse
        ? isInfixOf
        ? isPrefixOf
        ? isSuffixOf
        ? length
        ? map2
        ? mapWithIndex
        ? memberOf
        ? new
        ? partition
        ? patch
        ? put
        ? range
        ? reduceLeft
        ? repeat
        ? replace
        ? rotateLeft
        ? rotateRight
        ? sameElements
        ? scan
        ? scanLeft
        ? scanRight
        ? sortWith
        ? span
        ? takeLeft
        ? takeWhileLeft
        ? takeWhileRight
        ? toList
        ? toMap
        ? toMutList
        ? toSet
        ? transform
        ? transformWithIndex
        ? transpose
        ? update
        ? updateSequence

        # Underscore.js
        ? rest
        ? object

    - comment: Boolean methods
      name: support.function.boolean.sa
      match: !!set
        ? true
        ? false
        ? not
        ? and
        ? or
        ? xor
        ? nand
        ? nor
        ? xnor
        ? ifThen
        ? ifThenNot
        ? thenIf
        ? thenIfNot
        ? leftProj
        ? rightProj
        ? leftNotProj
        ? rightNotProj

    - comment: BigInt methods
      name: support.function.bigint.sa
      match: !!set
        ? abs
        ? clampToFloat32
        ? clampToFloat64
        ? clampToInt16
        ? clampToInt32
        ? clampToInt64
        ? clampToInt8
        ? clearBit
        ? compare
        ? dist
        ? flipBit
        ? fromString
        ? gcd
        ? getBit
        ? log2
        ? max
        ? min
        ? setBit
        ? signum
        ? toString
        ? tryToFloat32
        ? tryToFloat64
        ? tryToInt16
        ? tryToInt32
        ? tryToInt64
        ? tryToInt8

    - comment: Character methods
      name: support.function.character.sa
      match: !!set
        ? fromInt32
        ? isAscii
        ? isAsciiDigit
        ? isDigit
        ? isHexDigit
        ? isLetter
        ? isLowerCase
        ? isOctDigit
        ? isUpperCase
        ? isWhiteSpace
        ? toInt32
        ? toLowerCase
        ? toString
        ? toUpperCase

    - comment: Collection methods
      name: support.function.collection.sa
      match: !!set
        ? countBy
        ? each
        ? eachRight
        ? every
        ? filter
        ? find
        ? findLast
        ? flatMap
        ? flatMapDeep
        ? flatMapDepth
        ? forEach
        ? forEachRight
        ? groupBy
        ? includes
        ? invokeMap
        ? keyBy
        ? map
        ? orderBy
        ? partition
        ? reduce
        ? reduceRight
        ? reject
        ? sample
        ? sampleSize
        ? shuffle
        ? size
        ? some
        ? sortBy

        # Underscore.js
        ? where
        ? findWhere
        ? contains
        ? invoke
        ? pluck
        ? max
        ? min
        ? indexBy
        ? toArray

    - comment: DateTime methods
      name: support.function.datetime.sa
      match: !!set # date-fns
        ? add
        ? closestIndexTo
        ? closestTo
        ? compareAsc
        ? compareDesc
        ? format
        ? formatDistance
        ? formatDistanceStrict
        ? formatDistanceToNow
        ? formatDistanceToNowStrict
        ? formatDuration
        ? formatISO
        ? formatISO9075
        ? formatISODuration
        ? formatRFC3339
        ? formatRFC7231
        ? formatRelative
        ? intervalToDuration
        ? intlFormat
        ? isAfter
        ? isBefore
        ? isDate
        ? isEqual
        ? isExists
        ? isFuture
        ? isMatch
        ? validates
        ? isPast
        ? isValid
        ? lightFormat
        ? max
        ? min
        ? parse
        ? parseISO
        ? parseJSON
        ? set
        ? sub
        ? toDate
        ? daysToWeeks
        ? hoursToMilliseconds
        ? hoursToMinutes
        ? hoursToSeconds
        ? millisecondsToHours
        ? millisecondsToMinutes
        ? millisecondsToSeconds
        ? minutesToHours
        ? minutesToMilliseconds
        ? minutesToSeconds
        ? monthsToQuarters
        ? monthsToYears
        ? quartersToMonths
        ? quartersToYears
        ? secondsToHours
        ? secondsToMilliseconds
        ? secondsToMinutes
        ? weeksToDays
        ? yearsToMonths
        ? yearsToQuarters
        ? areIntervalsOverlapping
        ? clamp
        ? eachDayOfInterval
        ? eachHourOfInterval
        ? eachMinuteOfInterval
        ? eachMonthOfInterval
        ? eachQuarterOfInterval
        ? eachWeekOfInterval
        ? eachWeekendOfInterval
        ? eachYearOfInterval
        ? getOverlappingDaysInIntervals
        ? isWithinInterval
        ? fromUnixTime
        ? getTime
        ? getUnixTime
        ? addMilliseconds
        ? differenceInMilliseconds
        ? getMilliseconds
        ? milliseconds
        ? setMilliseconds
        ? subMilliseconds
        ? addSeconds
        ? differenceInSeconds
        ? endOfSecond
        ? getSeconds
        ? isSameSecond
        ? isThisSecond
        ? setSeconds
        ? startOfSecond
        ? subSeconds
        ? addMinutes
        ? differenceInMinutes
        ? endOfMinute
        ? getMinutes
        ? isSameMinute
        ? isThisMinute
        ? roundToNearestMinutes
        ? setMinutes
        ? startOfMinute
        ? subMinutes
        ? addHours
        ? differenceInHours
        ? endOfHour
        ? getHours
        ? isSameHour
        ? isThisHour
        ? setHours
        ? startOfHour
        ? subHours
        ? addBusinessDays
        ? addDays
        ? differenceInBusinessDays
        ? differenceInCalendarDays
        ? differenceInDays
        ? endOfDay
        ? endOfToday
        ? endOfTomorrow
        ? endOfYesterday
        ? getDate
        ? getDayOfYear
        ? isSameDay
        ? isToday
        ? isTomorrow
        ? isYesterday
        ? setDate
        ? setDayOfYear
        ? startOfDay
        ? startOfToday
        ? startOfTomorrow
        ? startOfYesterday
        ? subBusinessDays
        ? subDays
        ? getDay
        ? getISODay
        ? isFriday
        ? isMonday
        ? isSaturday
        ? isSunday
        ? isThursday
        ? isTuesday
        ? isWednesday
        ? isWeekend
        ? nextDay
        ? nextFriday
        ? nextMonday
        ? nextSaturday
        ? nextSunday
        ? nextThursday
        ? nextTuesday
        ? nextWednesday
        ? previousDay
        ? previousFriday
        ? previousMonday
        ? previousSaturday
        ? previousSunday
        ? previousThursday
        ? previousTuesday
        ? previousWednesday
        ? setDay
        ? setISODay
        ? addWeeks
        ? differenceInCalendarWeeks
        ? differenceInWeeks
        ? endOfWeek
        ? getWeek
        ? getWeekOfMonth
        ? getWeeksInMonth
        ? isSameWeek
        ? isThisWeek
        ? lastDayOfWeek
        ? setWeek
        ? startOfWeek
        ? subWeeks
        ? differenceInCalendarISOWeeks
        ? endOfISOWeek
        ? getISOWeek
        ? isSameISOWeek
        ? isThisISOWeek
        ? lastDayOfISOWeek
        ? setISOWeek
        ? startOfISOWeek
        ? addMonths
        ? differenceInCalendarMonths
        ? differenceInMonths
        ? eachWeekendOfMonth
        ? endOfMonth
        ? getDaysInMonth
        ? getMonth
        ? isFirstDayOfMonth
        ? isLastDayOfMonth
        ? isSameMonth
        ? isThisMonth
        ? lastDayOfMonth
        ? setMonth
        ? startOfMonth
        ? subMonths
        ? addQuarters
        ? differenceInCalendarQuarters
        ? differenceInQuarters
        ? endOfQuarter
        ? getQuarter
        ? isSameQuarter
        ? isThisQuarter
        ? lastDayOfQuarter
        ? setQuarter
        ? startOfQuarter
        ? subQuarters
        ? addYears
        ? differenceInCalendarYears
        ? differenceInYears
        ? eachWeekendOfYear
        ? endOfYear
        ? getDaysInYear
        ? getYear
        ? isLeapYear
        ? isSameYear
        ? isThisYear
        ? lastDayOfYear
        ? setYear
        ? startOfYear
        ? subYears
        ? addISOWeekYears
        ? differenceInCalendarISOWeekYears
        ? differenceInISOWeekYears
        ? endOfISOWeekYear
        ? getISOWeekYear
        ? getISOWeeksInYear
        ? isSameISOWeekYear
        ? lastDayOfISOWeekYear
        ? setISOWeekYear
        ? startOfISOWeekYear
        ? subISOWeekYears
        ? endOfDecade
        ? getDecade
        ? lastDayOfDecade
        ? startOfDecade
        ? getWeekYear
        ? setWeekYear
        ? startOfWeekYear

    - comment: Map methods
      name: support.function.map.sa
      match: !!set
        ? adjust
        ? adjustWithKey
        ? count
        ? delete
        ? difference
        ? differenceWith
        ? differenceWithKey
        ? empty
        ? exists
        ? filter
        ? filterWithKey
        ? find
        ? findLeft
        ? findRight
        ? fold
        ? foldLeft
        ? foldLeftWithKey
        ? foldRight
        ? foldRightWithKey
        ? foldWithKey
        ? forall
        ? foreach
        ? get
        ? getWithDefault
        ? insert
        ? insertWith
        ? insertWithKey
        ? intersection
        ? intersectionWith
        ? intersectionWithKey
        ? isEmpty
        ? isProperSubmapOf
        ? isSubmapOf
        ? keysOf
        ? map
        ? mapWithKey
        ? memberOf
        ? query
        ? reduce
        ? reduceLeft
        ? reduceLeftWithKey
        ? reduceRight
        ? reduceRightWithKey
        ? reduceWithKey
        ? singleton
        ? size
        ? toList
        ? toMut
        ? toSet
        ? unfold
        ? unfoldWithIter
        ? union
        ? unionWith
        ? unionWithKey
        ? update
        ? updateWithKey
        ? valuesOf

    - comment: Number methods
      name: support.function.number.sa
      match: !!set # JavaScript
        ? isFinite
        ? isInteger
        ? isNaN
        ? isSafeInteger
        ? parseFloat
        ? parseInt
        ? toExponential
        ? toFixed
        ? toLocaleString
        ? toPrecision
        ? toSource
        ? toString
        ? valueOf

    - comment: Function methods
      name: support.function.function.sa
      match: |
        (?x)\b(
        apply|bind|call|next|return|throw|maybeCall|callOrReturn|boundMethod|converge|comply
        |composeApply|defer|debounceAsync|flurry|flowCurry|mapArgs|differentLast|overNone
        |ifElse|when(Truthy|Exists)|unless|flowMap|findApply|insertAtIndex|(compact|deep)Map|map
        |after|ary|before|bind(Key)|curry(Left|Right)?|debounce|defer|delay|flip|memoize|negate|once
        |overArgs|partial(Left|Right)?|rearg|rest|spread|throttle|(u|bi|ter)?nary|wrap
        )\b

    - comment: Non-empty list methods
      name: support.function.nel.sa
      match: !!set # Flix
        ? append
        ? cons
        ? count
        ? exists
        ? filter
        ? find
        ? findLeft
        ? findRight
        ? flatMap
        ? flatten
        ? fold
        ? foldLeft
        ? foldRight
        ? forall
        ? foreach
        ? head
        ? init
        ? intersperse
        ? last
        ? length
        ? map
        ? mapWithIndex
        ? memberOf
        ? permutations
        ? reduce
        ? reduceLeft
        ? reduceRight
        ? replace
        ? reverse
        ? singleton
        ? subsequences
        ? tail
        ? toList
        ? unzip
        ? zip
        ? zipWith

    - comment: Regular expression methods
      name: support.function.regex.sa
      match: |
        (?x)\b(
        exec|test|(match|replace)(All)?|search|split|(add|all|del|[gs]et)Tokens?
        |build|cache|escape|forEach|globalize|isRegExp?|match(Chain|Recursive)
        |replace(Each)?|split|tag|test|union|intersect|diff(erence)?|symmetric
        |source|flags|groups|captures
        )

    - comment: Object methods
      name: support.function.object.sa
      match: !!set # Lodash
        ? assign
        ? assignIn
        ? assignInWith
        ? assignWith
        ? at
        ? create
        ? defaults
        ? defaultsDeep
        ? entries
        ? entriesIn
        ? extend
        ? extendWith
        ? findKey
        ? findLastKey
        ? forIn
        ? forInRight
        ? forOwn
        ? forOwnRight
        ? functions
        ? functionsIn
        ? get
        ? has
        ? hasIn
        ? invert
        ? invertBy
        ? invoke
        ? keys
        ? keysIn
        ? mapKeys
        ? mapValues
        ? merge
        ? mergeWith
        ? omit
        ? omitBy
        ? pick
        ? pickBy
        ? result
        ? set
        ? setWith
        ? toPairs
        ? toPairsIn
        ? transform
        ? unset
        ? update
        ? updateWith
        ? values
        ? valuesIn

        # Underscore.js
        ? allKeys
        ? mapObject
        ? pairs
        ? extendOwn
        ? clone
        ? tap
        ? toPath
        ? property
        ? propertyOf
        ? matcher
        ? isEqual
        ? isMatch
        ? isEmpty
        ? isElement
        ? isArray
        ? isObject
        ? isArguments
        ? isFunction
        ? isString
        ? isNumber
        ? isFinite
        ? isBoolean
        ? isDate
        ? isRegExp
        ? isError
        ? isSymbol
        ? isMap
        ? isWeakMap
        ? isSet
        ? isWeakSet
        ? isArrayBuffer
        ? isDataView
        ? isTypedArray
        ? isNaN
        ? isNull
        ? isUndefined

    - comment: Tree methods
      name: support.function.tree.sa
      match: !!set # Flix
        ? delete
        ? empty
        ? exists
        ? findLeft
        ? findRight
        ? foldLeft
        ? foldRight
        ? forall
        ? foreach
        ? get
        ? insert
        ? insertWith
        ? memberOf
        ? queryWith
        ? queryWithHelper
        ? reduceLeft
        ? reduceRight
        ? size
        ? updateWith

    - comment: Result methods
      name: support.function.result.sa
      match: !!set # Flix
        ? ap
        ? count
        ? exists
        ? find
        ? flatMap
        ? foldLeft
        ? foldLeftM
        ? foldRight
        ? foldRightM
        ? forall
        ? foreach
        ? getWithDefault
        ? isErr
        ? isOk
        ? lift10
        ? lift2
        ? lift3
        ? lift4
        ? lift5
        ? lift6
        ? lift7
        ? lift8
        ? lift9
        ? map
        ? mapErr
        ? replace
        ? sequence
        ? toList
        ? toMap
        ? toOption
        ? toSet
        ? traverse
        ? traverseX
        ? withDefault
        ? filter
        ? flatMap2
        ? flatten
        ? isEmpty
        ? map2
        ? toErr
        ? toFailure
        ? toOk
        ? toSuccess
        ? unzip
        ? zip

    - comment: Iterator methods
      name: support.function.sequence.sa
      match: !!set # itt (JS)
        ? from
        ? empty
        ? range
        ? irange
        ? replicate
        ? forever
        ? iterate
        ? split
        ? exec
        ? entries
        ? keys
        ? values
        ? is
        ? generator
        ? slice
        ? drop
        ? dropWhile
        ? dropLast
        ? take
        ? takeWhile
        ? takeLast
        ? tail
        ? init
        ? map
        ? flatMap
        ? filter
        ? reject
        ? partition
        ? scan
        ? scan1
        ? first
        ? last
        ? pick
        ? count
        ? every
        ? some
        ? tap
        ? detect
        ? find
        ? findLast
        ? findIndex
        ? findLastIndex
        ? indexOf
        ? lastIndexOf
        ? includes
        ? cartesianProduct
        ? permutations
        ? combinations
        ? enumerate
        ? intersperse
        ? cycle
        ? repeat
        ? unique
        ? flatten
        ? chunksOf
        ? chunksBy
        ? subsequences
        ? lookahead
        ? transpose
        ? concat
        ? zip
        ? parallel
        ? push
        ? unshift
        ? reduce
        ? reduce1
        ? inject
        ? sum
        ? mean
        ? product
        ? max
        ? min
        ? minMax
        ? join
        ? groupBy
        ? keyBy
        ? forEach
        ? drain
        ? toArray
        ? toSet
        ? toMap
        ? toObject
        ? fork

    - comment: Set methods
      name: support.function.set.sa
      match: !!set # Flix
        ? count
        ? delete
        ? difference
        ? empty
        ? exists
        ? filter
        ? find
        ? findLeft
        ? findRight
        ? flatMap
        ? flatten
        ? fold
        ? foldLeft
        ? foldRight
        ? forall
        ? foreach
        ? insert
        ? intersection
        ? isEmpty
        ? isProperSubsetOf
        ? isSubsetOf
        ? map
        ? memberOf
        ? partition
        ? query
        ? range
        ? reduce
        ? reduceLeft
        ? reduceRight
        ? replace
        ? singleton
        ? size
        ? subsets
        ? toList
        ? toMap
        ? toMut
        ? unfold
        ? unfoldWithIter
        ? union

    - comment: String methods
      name: support.function.string.sa
      match: !!set # JavaScript
        ? anchor
        ? at
        ? big
        ? blink
        ? bold
        ? charAt
        ? charCodeAt
        ? codePointAt
        ? concat
        ? endsWith
        ? fixed
        ? fontcolor
        ? fontsize
        ? fromCharCode
        ? fromCodePoint
        ? includes
        ? indexOf
        ? italics
        ? lastIndexOf
        ? link
        ? localeCompare
        ? match
        ? matchAll
        ? normalize
        ? padEnd
        ? padStart
        ? raw
        ? repeat
        ? replace
        ? replaceAll
        ? search
        ? slice
        ? small
        ? split
        ? startsWith
        ? strike
        ? sub
        ? substr
        ? substring
        ? sup
        ? toLocaleLowerCase
        ? toLocaleUpperCase
        ? toLowerCase
        ? toSource
        ? to
        ? toUpperCase
        ? trim
        ? trimEnd
        ? trimStart
        ? valueOf

        # Flix
        ? abbreviateLeft
        ? abbreviateRight
        ? breakOnLeft
        ? breakOnRight
        ? commonPrefix
        ? commonSuffix
        ? contains
        ? countSubstring
        ? drop
        ? dropLeft
        ? dropRight
        ? dropWhile
        ? dropWhileAround
        ? dropWhileLeft
        ? dropWhileRight
        ? exists
        ? findIndexOfLeft
        ? findIndexOfRight
        ? findIndices
        ? flatten
        ? foldLeft
        ? foldLeft2
        ? foldRight
        ? foldRight2
        ? forall
        ? indent
        ? indexOfLeft
        ? indexOfLeftWithOffset
        ? indexOfRight
        ? indexOfRightWithOffset
        ? indices
        ? init
        ? intercalate
        ? intercalateChar
        ? isAscii
        ? isEmpty
        ? isMatch
        ? isSubmatch
        ? isWhiteSpace
        ? length
        ? levenshteinDistance
        ? lines
        ? lineSeparator
        ? map
        ? map2
        ? mapWithIndex
        ? padLeft
        ? padRight
        ? patch
        ? replaceChar
        ? replaceFirstMatch
        ? replaceMatches
        ? reverse
        ? rotateLeft
        ? rotateRight
        ? sliceLeft
        ? sliceRight
        ? splitAt
        ? splitOn
        ? stripIndent
        ? stripPrefix
        ? stripSuffix
        ? take
        ? takeLeft
        ? takeRight
        ? takeWhile
        ? takeWhileLeft
        ? takeWhileRight
        ? toArray
        ? toChunks
        ? toList
        ? trimLeft
        ? trimRight
        ? unfold
        ? unfoldString
        ? unfoldStringWithIter
        ? unfoldWithIter
        ? unlines
        ? unwords
        ? update
        ? words
        ? zip
        ? zipWith

    - comment: Channel methods
      name: support.function.channel.sa
      match: !!set # Flix
        ? get
        ? new
        ? newWithCapacity
        ? put

    - comment: DataView methods
      name: support.function.dataview.sa
      match: |
        (?x)\b(
        [gs]et(S?Byte|U?(Short|Int|Long|Cent)|U?Int(8|16|32|64|128)
        |Float(16|32|64|128)?|Double|Decimal|Big(Int|Deci)|Fraction|Real|Imaginary|Complex)
        )\b

    - comment: Atomics methods
      name: support.function.atomics.sa
      match: !!set # JavaScript
        ? not
        ? and
        ? or
        ? xor
        ? nand
        ? nor
        ? xnor
        ? add
        ? sub
        ? notify
        ? load
        ? store
        ? wait
        ? exchange
        ? isLockFree
        ? compareExchange

constant-names:
  comment: Names for constants, built-in types and static properties
  patterns:
    - match: |
        (?x)

variable-names:
  comment: Standard Library legacy variable names
  patterns:
    - match: |
        (?x)

property-names:
  comment: Standard Library property names
  patterns:
    - match: |
        (?x)
